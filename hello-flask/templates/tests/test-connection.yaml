apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "hello-flask.fullname" . }}-test-connection"
  labels:
    {{- include "hello-flask.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "hello-flask.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
