apiVersion: v1
kind: Service
metadata:
  name: {{ include "hello-flask.fullname" . }}
  labels:
    {{- include "hello-flask.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "hello-flask.selectorLabels" . | nindent 4 }}
